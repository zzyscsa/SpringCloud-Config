spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/zzyscsa/config-repo.git
          username: ********
          password: ********
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/